#include "check.h"

int check_tag(TIFF* tif, int tag) {
  /* TODO */
  printf("check if tag %i exists\n", tag);
  return 0;
}
int check_notag(TIFF* tif, int tag) {
  /* TODO */
  printf("check if tag %i does not exist\n", tag);
  return 0;
}

